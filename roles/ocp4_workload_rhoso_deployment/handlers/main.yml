---
# Handler is a no-op since NFS restart is now handled inline in nfs_server.yml
# via SSH through the bastion. Kept for backwards compatibility.
- name: Restart nfs-server
  ansible.builtin.debug:
    msg: "NFS server restart handled inline via bastion SSH."
  delegate_to: bastion
